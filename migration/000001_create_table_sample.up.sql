CREATE TABLE IF NOT EXISTS samples (
                                      id bigserial NOT NULL,
                                      name text,
                                      created_at timestamp,
                                      updated_at timestamp    );